# Pioneer hack

This is the first commit

# Instruction

## Server
  - currently in `./server.js`
  - Run server

    `$ npm install` (maybe)

    `$ node server.js`

## Client
  - All in `./client/`
  - Run client

    `$ npm install` (maybe)

    `$ cd client && yarn start`
